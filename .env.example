# Local Docker compose (docker-compose.yml)
POSTGRES_USER=wishlist
POSTGRES_PASSWORD=wishlist
POSTGRES_DB=wishlist

# Production compose only (docker-compose.production.yml)
IMAGE_TAG=latest

# Playwright, when the e2e suite runs against something other than the test stack
BASE_URL=http://localhost:5173
